# Copy to .env.local for development. The default matches the Django dev
# server, so a local checkout needs no edits.
#
# NEXT_PUBLIC_ is required: the storefront calls the API from the browser, so
# this value is compiled into the client bundle. Never put a secret here.
NEXT_PUBLIC_API_URL=http://localhost:8000
